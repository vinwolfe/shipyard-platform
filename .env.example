# =========================
# Shipyard local development
# =========================

# Service config
SERVICE_PORT=8080

# Postgres (local host). Use:
# - `make db-up` if you have compose postgres running on localhost:5432
# - or your own local Postgres instance
DATABASE_URL=postgres://shipyard:shipyard@localhost:5432/shipyard

# Observability (optional for local cargo dev; required if you want traces exported)
# OTEL_SERVICE_NAME=fulfilment-api
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:14317

# Logging
RUST_LOG=info,fulfilment_api=info,shipyard_web=info,tower_http=warn