[package]
name = "fulfilment-api"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }

[dev-dependencies]
tower = "0.5"
http-body-util = "0.1"
hyper = { version = "1", features = ["client"] }